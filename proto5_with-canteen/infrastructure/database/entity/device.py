class Device:
    def __init__(self, dev_id, dev_name, sr_no, mac_id, ip_add, dir_flag, def_mode, mesh_id):
        self.dev_id = dev_id
        self.dev_name = dev_name
        self.sr_no = sr_no
        self.mac_id = mac_id
        self.ip_add = ip_add
        self.dir_flag = dir_flag
        self.def_mode = def_mode
        self.mesh_id = mesh_id
